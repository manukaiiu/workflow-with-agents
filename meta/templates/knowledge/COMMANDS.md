# Useful Commands

**Last Updated**: [Date]
**Updated By**: [Human/Agent]

> Project-specific commands and gotcha-prone operations. Generic commands (git, npm basics) don't need to be documented here.

## Build & Development

```bash
# Start development server
[command]

# Production build
[command]

# Run linting
[command]

# Run type checking
[command]
```

## Testing

```bash
# Run all tests
[command]

# Run tests in watch mode
[command]

# Run specific test file
[command]

# Run tests with coverage
[command]
```

## Database

```bash
# Run migrations
[command]

# Seed database
[command]

# Reset database
[command]

# Generate types/client
[command]
```

## Package Management

```bash
# Install dependencies
[command]

# Add new dependency
[command]

# Update dependencies
[command]
```

## Gotcha-Prone Operations

### [Operation Name]
**Why it's tricky**: [Explanation]
```bash
# Correct way to do it
[command]
```

### [Another Operation]
**Why it's tricky**: [Explanation]
```bash
# Correct way to do it
[command]
```

## Environment-Specific

### Local Development
```bash
[commands specific to local dev]
```

### CI/CD
```bash
[commands specific to CI/CD]
```

## Debugging

```bash
# Check for common issues
[command]

# View logs
[command]

# Debug mode
[command]
```

---

**Note**: Keep this focused on project-specific commands. If a command is standard and well-known, it doesn't need to be here.
